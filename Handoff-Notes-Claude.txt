Phase 1 complete. Files edited:
styles/main.css

Task 1: Removed .btn, .btn-primary, .btn-secondary, .btn-close blocks. Replaced with button {} / button:hover {} / button:active {} (exact gold-on-black neumorphic style you provided). Updated .btn-pill to match the same style for <a> elements. Removed old .btn mobile override.
Task 3: Changed body, p, .body-text, input, .error-message font-family from 'Cormorant Garamond'/'Inter' to 'DM Sans', sans-serif. Titles/headers (h1–h3, label, eyebrow, section-label, site-nav) are untouched.
Task 4: Added ul li::marker cycling rules — Gold → Deep Electric Purple → Field Green → Hot Magenta (from the palette zine Rules section).
styles/what-is-soul-school.css

Task 1b: Updated .dot class with explicit resets (height:6px; width:6px; margin:0; box-shadow:none; font-size:0) so the tiny nav dots don't inherit the 120×50px button {} size.
styles/calendar.css

Task 1c: Updated .month-btn / .month-btn:hover with explicit resets so the circular calendar nav buttons keep their 38×38px circular appearance.
index.html

Task 2: Changed the button wrapper div from display:flex; flex-wrap:wrap to display:flex; flex-direction:column; align-items:flex-start — buttons now stack vertically, left-aligned.
pages/what-is-soul-school.html

Task 5: Both RADICAL instances replaced with per-letter rainbow spans: R(red) A(orange) D(yellow) I(green) C(blue) A(purple) L(violet).
zines/soul_school_font_zine.html

Task 5b: The one plain-text RADICAL replaced with the same per-letter rainbow spans.
Global button CSS updated. Fonts, bullets, RADICAL rainbow done.

Next: Phase 2 — index.html restructure and enter.html content changes.


Phase 2 complete. Files edited:
All 7 tasks are complete. Here's a summary of what was changed:

index.html

Removed the "What is Soul School?" button (2 buttons remain)
Removed min-height:100vh from the hero section and reduced padding so the gatherings section is visible without scrolling
Renamed "UPCOMING GATHERINGS" → "GATHER TOGETHER" with subheading "The best way to understand Soul School is to show up."
Removed "Retreat Orientation" placeholder (only Popups + Office Hours remain); added <!-- CALENDAR: show next 3 public events only --> comment
pages/enter.html

Added the HOW ACCESS WORKS block above the password input, including RADICAL styled with the .radical rainbow gradient class
pages/what-is-soul-school.html

Removed the HOW ACCESS WORKS section and its trailing gold rule divider
Added a <script> block before <body> that redirects to enter.html if soulschool_auth is not set in sessionStorage
The enter.html password form already sets sessionStorage.setItem('soulschool_auth', 'true') on success, so the gate is complete end-to-end.
index.html restructured, enter.html updated, password gate on 
what-is-soul-school.html. 
Next: Phase 3 — Protected page (inside Soul School) content structure.

Edit: reverted changes on button logic due to redundancy of code and incorrect application. Will revisit in a later phase. 

Phase 3 complete. Files edited:
Files edited:

File	What changed
pages/soul-school-portal.html	Full restructure — replaced old 3-section page with 6 ordered sections: Calendar Block, Is Soul School For Me, Soul Practices, Initiation Call Booking, Story of Why, Advisory Council
styles/soul-school-portal.css	Populated the previously empty file with all portal-specific styles: week grid, pill event cards, practice cards, SoundCloud placeholders, booking placeholder, Story of Why layout, mobile breakpoints
pages/enter.html	Added one line to store soulschool_entry_date as an ISO string in sessionStorage at the moment of successful login
Key notes per section:

Calendar — JS renders a 7-day grid starting from soulschool_entry_date (set at login), falling back to today if not present. Pill-style upcoming gatherings use placeholders with a Google Calendar TODO comment.

Is Soul School For Me — pulled verbatim from the c6 slide's left column in what-is-soul-school.html.

Soul Practices — "Try a Soul Practice!" heading, 3 active cards with <!-- SOUNDCLOUD: embed for [Name] --> placeholders, Attention Sovereignty and Creative Transmission commented out with <!-- COMING SOON: will become hyperlink teasers -->.

Booking — <!-- BOOKING: embed link here --> placeholder inside a styled dashed-border container.

Story of Why — 4 static sections (Era We're Entering, What's Broken, Soul School Response, Paradigm Shift) from the carousel, rendered as normal scrollable content, no JS needed.

Advisory Council — moved intact from what-is-soul-school.html with all headshots, names, titles, and bios preserved.


Protected page fully structured with all 6 sections.
Next: Phase 4 — Google Calendar setup instructions + Member Login placeholder.



4-2NotesUpdates Branch:
Phase 1:
Files Edited:

styles/main.css — updated Google Fonts import (Josefin Sans + Playfair Display only), set body font to Josefin Sans, updated all component font references from DM Sans/Cinzel to Josefin Sans, kept Playfair Display exclusively on .site-nav-logo, .italic-gold, .site-footer-logo
styles/what-is-soul-school.css — added @import url('main.css'), extracted all inline styles into named classes
index.html — removed all inline styles and <style> blocks, removed both gold-rule divider elements, switched font preload to styles/index.css
pages/enter.html — removed all inline styles, replaced with classes, linked enter.css
pages/what-is-soul-school.html — stripped all inline styles, replaced with semantic class names, linked what-is-soul-school.css
pages/member-portal.html — was empty; created clean shell linking member-portal.css
Created:

styles/index.css — hero, tagline, gather section, event pill styles; imports main.css
styles/enter.css — enter page layout, access form, input, error message styles; imports main.css
styles/member-portal.css — shell with @import url('main.css')
styles/archive.css — shell with @import url('main.css')

Phase 2:
index.html — rebuilt body content (hero, CTAs, tagline block, events section; removed GATHER TOGETHER heading)
styles/index.css — replaced all styles with new layout classes; added .rainbow-text gradient utility, .hero centering, .tagline-block, .event-cards grid, .event-card and its children, responsive breakpoint
Key details:
.rainbow-text uses a static left-to-right rainbow via background-clip: text / -webkit-text-fill-color: transparent — applied to "love, joy, and liberation" in the tagline and "RADICAL" in the tagline block
CTA buttons are now in a row (flex-direction: column) and centered
Events use a 3-column CSS grid that collapses to 1-column below 900px
The <!-- TODO: Replace with Google Calendar integration --> comment is inside .events-section

Phase 3:
Files edited:
pages/enter.html	Stripped all body copy and the "HOW ACCESS WORKS" block. Left only: site nav, centered access code input + submit button, WEAREFREE validation logic. Redirect on success now points to what-is-soul-school.html. Added <!-- TODO: Future video + animation with soundtrack goes here --> placeholder.
styles/enter.css	Removed .enter-main .body-text, .enter-hint, .access-block, .access-block .stencil, .access-block .body-text, .access-divider — all styles for stripped elements.
pages/what-is-soul-school.html	Full rewrite. Auth gate preserved. Content top-to-bottom: site nav → hero heading/subheading → 6 event cards → scrollable image carousel (placeholder boxes wired to assets/carousel-images/) → YouTube embed (OdUoW1oihdQ) with "Video by AJ" credit → We Are Free image (assets/We-are-free-GOLD.png) with filename comment → <!-- TODO: SoundCloud embed --> comment → Book a Call section → footer.
styles/what-is-soul-school.css	Full rewrite. Old carousel/slide/advisory styles removed. New styles for: .wiss-hero, .wiss-heading, .wiss-subheading, event card grid (matching home page), .wiss-carousel-section + placeholder boxes, .wiss-video-section + 16:9 iframe wrap + credit, .wiss-wearefree-section, .wiss-booking-section. Responsive breakpoint at 900px.

Phase 4:
Files edited / created:
pages/archive.html	New — copy of the old soul-school-portal.html (calendar, practices, advisory council content). Content unchanged.
pages/soul-school-portal.html	Renamed from what-is-soul-school.html. Title tag updated to "Soul School Portal". CSS link and auth gate unchanged.
pages/enter.html	Redirect on success updated: what-is-soul-school.html → soul-school-portal.html.
movement/main.js	Both soul-school-portal.html redirect targets updated → archive.html (the old portal is now archived).
pages/member-portal.html	Replaced empty shell with a styled "under construction" page: site nav, heading, subtext, back link. No inline styles.
styles/member-portal.css	Added .under-construction-main, .under-construction-heading, .under-construction-sub styles.
CSS verification:

enter.html → enter.css → @import main.css ✓
soul-school-portal.html → what-is-soul-school.css → @import main.css ✓
member-portal.html → member-portal.css → @import main.css ✓
index.html → index.css → @import main.css ✓
archive.html → links main.css + soul-school-portal.css directly ✓

Phase 5:
Files edited:

File	What changed
styles/main.css	Background: replaced flat background on body with background-color: var(--void) + a body::before fixed pseudo-element combining the diagonal gradient with a tiling SVG noise grain at 4% opacity. Playfair audit: .italic-gold → Josefin Sans italic 300. .site-footer-logo → Josefin Sans italic 300. .site-nav-logo left untouched (the one permitted use).
styles/index.css	Kerning: .event-title 0.06em → 0.02em. .event-meta 0.1em → 0.06em. .event-location 0.06em → 0.02em. .event-desc 0.04em → 0.
styles/what-is-soul-school.css	Kerning: same reductions mirrored from index.css. Mobile: added @media (max-width: 600px) breakpoint — carousel boxes shrink to 80vw, video section gets full-bleed (border-radius/padding removed), We Are Free image max-width 100%.
styles/calendar.css	Playfair audit: .pill-event-title → Josefin Sans italic 300.
styles/soul-school-portal.css	Playfair audit: .portal-section-heading, .pill-event-title, .portal-practice-cta, .portal-practice-name → all switched to Josefin Sans italic 300.
Playfair Display remaining uses (intentional):

main.css → .site-nav-logo only ✓

Background Gradient follow up:
Root cause: The gradient was on a position: fixed; z-index: -1 pseudo-element, but every page section had its own background: var(--void) painted on top of it, completely blocking it.

Fix applied across these files:

File	Change
styles/main.css	body gets the gradient directly via background + background-attachment: fixed. Removed the ::before pseudo-element entirely. Nav gets rgba(10,10,8,0.72) + backdrop-filter: blur(12px) so it's readable without killing the gradient behind it. Footer set to transparent.
styles/index.css	All background: var(--void) on sections → transparent.
styles/enter.css	Same.
styles/member-portal.css	Same.
styles/what-is-soul-school.css	Same.


Phase 7:
All four existing files are updated. Here's a summary of what was done and one thing to flag:

index.html	Replaced <title> + old description; added keywords, author, Open Graph, and Twitter Card meta tags
pages/enter.html	Replaced description; added OG title, url, type, image tags
pages/member-portal.html	Updated <title> to "Soul School Member Portal"; added noindex, nofollow
pages/archive.html	Updated <title> to "Soul School — Archive"; added noindex, nofollow
One thing to flag: pages/what-is-soul-school.html does not exist in the repo — only these files are in pages/: enter.html, member-portal.html, archive.html, soul-school-portal.html, calendar.html. If what-is-soul-school.html needs to be created or if it's actually one of these other files under a different name, let me know and I'll handle it.

All existing <head> sections already had both <meta charset="UTF-8"> and <meta name="viewport" content="width=device-width, initial-scale=1.0"> — no changes needed there.

Phase 8:

index.html
Added <h1 class="visually-hidden">Soul School</h1> inside .hero-logo-wrap — establishes the single required <h1> without changing the visual layout (hidden via CSS)
alt="Soul School logo" → alt="Soul School wordmark in gold" on SOUL-SCHOOL-HEADER-GOLD.png
<p class="tagline-show-up">SHOW UP.</p> → <h2> — promotes the section header to the correct semantic level
All three <h3 class="event-title"> → <h2> (Soul School Bus Popup, Office Hours with Annie, Full Moon Gathering) — also fixed the mismatched </h3> closing tags left over from the original
pages/soul-school-portal.html
All six <h3 class="event-title"> → <h2> — also fixed all six mismatched </h3> closing tags
alt="WE ARE FREE TODAY Grass picture" → alt="Soul School community gathering" on carousel image
alt="We Are Free We Are Free We Are Free" → alt="We Are Free — Soul School community" on We-are-free-GOLD.png
<p class="wiss-booking-prompt"> → <h2> on "If you are ready, let us talk." — establishes semantic heading for the Book a Call section
pages/archive.html
alt="Advisory Council" → alt="We Are Free — Soul School community" on We-are-free-GOLD.png
styles/main.css
Added .visually-hidden utility class — standard screen-reader-visible / visually-hidden pattern, required to support the new <h1> in index.html without affecting layout
No changes needed
carousel.js — does not inject <img> tags via JS template strings; it only manipulates pre-existing DOM slides. The one live carousel image is statically in the HTML and was updated there.
pages/archive.html section 4 — "Book an Initiation Call" was already an <h2>
pages/enter.html, pages/member-portal.html — no <img> tags, heading structure already correct

